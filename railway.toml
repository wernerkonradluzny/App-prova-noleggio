# Do not put another `npm ci` here. Nixpacks already installs in its
# install phase; a second ci is what crashed the last Railway build.
[deploy]
startCommand = "npx serve -s dist -l $PORT"
healthcheckPath = "/"
