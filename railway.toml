# Railway watches this GitHub repo. These two commands are what it needs:
# a build that still has Vite available, and a process that stays up on $PORT.
[build]
buildCommand = "npm ci --include=dev && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
